[package]
name = "sps"
version = "0.1.0"
edition = "2024"

[dependencies]
assert_cmd = "2.1.2"
clap = { version = "4.5.57", features = ["derive"] }
time = { version = "0.3.47", features = ["parsing"] }
